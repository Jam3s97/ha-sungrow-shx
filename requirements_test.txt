-r requirements_dev.txt

# Pinned to the same Home Assistant release as requirements_dev.txt (see
# https://pypi.org/project/pytest-homeassistant-custom-component/ for the
# version <-> homeassistant-release mapping); bump both together.
pytest-homeassistant-custom-component==0.13.363

# Our manifest declares a hard dependency on HA core's "modbus" component
# (see CLAUDE.md); its manifest in turn requires pymodbus even though this
# integration only ever goes through the tmodbus-backed shared connection
# pool -- HA installs it automatically at runtime, but the test environment
# needs it pinned explicitly. Keep this in sync with
# homeassistant/components/modbus/manifest.json for the pinned HA version.
pymodbus==3.15.0
